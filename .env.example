# Copy to .env.local for local dev; set the same two variables in Vercel for production.
# Both values come from your Supabase project: Dashboard → Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
